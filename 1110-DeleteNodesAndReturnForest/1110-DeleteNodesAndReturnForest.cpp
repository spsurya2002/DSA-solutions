           if(s.find(root->val)!=s.end()){
            if(root->left!=NULL)
           root->right= del(root->right,s,ans);
           root->left= del(root->left,s,ans);
        if(root==NULL)return NULL;
    TreeNode* del(TreeNode* root,unordered_set<int>s,vector<TreeNode*>&ans){
public:
class Solution {
 */
 * };
[
