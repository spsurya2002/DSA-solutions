}
    ListNode* doubleIt(ListNode* head) {
        ListNode* temp=head;
    return result;
    
    }
        result = std::to_string(sum) + result;
        sum %= 10;
        carry = sum / 10;
        }
            j--;
            sum += num2[j] - '0';
        if (j >= 0) {
        }
            i--;
            sum += num1[i] - '0';
        if (i >= 0) {
        int sum = carry;
    while (i >= 0 || j >= 0 || carry) {
    
    int j = num2.size() - 1;
    int i = num1.size() - 1;
    int carry = 0;
[
