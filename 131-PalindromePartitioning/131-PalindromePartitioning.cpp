            myres.push_back(temp);
        }

        return myres;
    }

    vector<vector<string>> partition(string s) {
        vector<deque<string>> ds = find(s);
"aab"
"a"
[["a","a","b"],["aa","b"]]
[["a"]]
[["a","a","b"],["aa","b"]]
[["a"]]
"
