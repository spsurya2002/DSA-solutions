        }

        temp=new ListNode();
        ListNode* ans=temp;
        for(auto x:st){
            ListNode* nnd=new ListNode(x);
            temp->next=nnd;
            temp=temp->next;
        }
        return ans->next;
    }
};
            if(x.second==1)st.insert(x.first);
        for(auto x:mp){
        }
            temp=temp->next;
           
[1,2,3,3,4,4,5]
[1,1,1,2,3]
[1,2,5]
[2,3]
[1,2,5]
[2,3]
[
